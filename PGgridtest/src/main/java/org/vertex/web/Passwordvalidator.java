package org.vertex.web;

import java.net.PasswordAuthentication;

public class Passwordvalidator {

	// private final String username = "karthik.k@vertexcs.com"; // like
	// yourname@outlook.com
	// private final String password = "Vertex@1";

	/*
	 * public String PasswordAuthentication getPasswordAuthentication() { // return
	 * null because we havent set the server
	 * 
	 * String promptString = ""; System.out.println(promptString); String hostname =
	 * this.getRequestingHost(); System.out.println(hostname);
	 * 
	 * 
	 * 
	 * 
	 * 
	 * return new PasswordAuthentication(username, password.toCharArray()); }
	 */
}
